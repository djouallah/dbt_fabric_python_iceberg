aemo_electricity:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: ':memory:'
      database: onelake
      threads: 1
      schema: "{{ env_var('DBT_SCHEMA', 'mart') }}"
      extensions:
        - azure
        - iceberg
        - avro
      settings:
        preserve_insertion_order: false
        memory_limit: '5GB'                 # stay under the ~7GB CI runner so DuckDB spills instead of OOM-killing
        temp_directory: '/tmp/duckdb_spill' # enable disk spilling for the :memory: database
        max_temp_directory_size: '30GB'
      secrets:
        - type: azure
          name: onelake_storage
          provider: access_token
          access_token: "{{ env_var('ONELAKE_TOKEN') }}"
      attach:
        - path: "{{ env_var('WAREHOUSE_PATH') }}"
          alias: onelake
          type: iceberg
          options:
            endpoint: "{{ env_var('ONELAKE_ENDPOINT') }}"
            token: "{{ env_var('ONELAKE_TOKEN') }}"
            access_delegation_mode: 'none'
            stage_create_tables: 0
            skip_create_table_metadata_updates: 1
            default_schema: dbo
